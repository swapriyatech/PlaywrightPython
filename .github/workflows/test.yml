name: test

on:
  workflow_dispatch:
    inputs:
      application:
        description: Application manifest name
        required: true
        default: app1
        type: choice
        options: [app1, app2]
      environment:
        description: Target environment
        required: true
        default: UAT
        type: choice
        options: [QA, UAT]
      suite:
        description: Suite marker
        required: true
        default: regression
        type: choice
        options: [smoke, sanity, regression]
      browser:
        description: Playwright browser
        required: true
        default: chromium
        type: choice
        options: [chromium, firefox, webkit]
  push:
    branches: [main]
  schedule:
    - cron: '0 1 * * *'

jobs:
  framework:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chromium, firefox, webkit]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.lock
      - run: pip install --no-deps -e .
      - run: playwright install --with-deps ${{ matrix.browser }}
      - name: Framework tests
        run: python -m pytest tests/unit tests/architecture --cov=src --cov-report=term-missing --junitxml=reports/junit.xml --html=reports/report.html --self-contained-html --alluredir=reports/allure
      - name: Browser tests
        env:
          APP: ${{ inputs.application || 'app1' }}
          ENVIRONMENT: ${{ inputs.environment || 'UAT' }}
          SUITE: ${{ inputs.suite || 'smoke' }}
          BROWSER: ${{ matrix.browser }}
          RUN_BROWSER_TESTS: 'true'
        run: python -m pytest tests/e2e -m "${{ inputs.application || 'app1' }} and ${{ inputs.suite || 'smoke' }}" --junitxml=reports/e2e-${{ matrix.browser }}.xml --html=reports/e2e-${{ matrix.browser }}.html --self-contained-html --alluredir=reports/allure
      - run: ruff check .
      - run: pip-audit -r requirements.lock --format cyclonedx-json --output reports/sbom.json
      - run: bandit -r src
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: reports-${{ matrix.browser }}
          path: reports/
          retention-days: 30
